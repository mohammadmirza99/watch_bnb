<%= simple_form_for [@watch, @request] do |f| %>

<%# Added a date picker %>
  <%= f.input :date_start, as: :date, order: [:day, :month, :year] %>
  <%= f.input :date_end, as: :date, order: [:day, :month, :year] %>

  <%= f.submit "Book", class: "btn btn-primary" %>
<% end  %>

